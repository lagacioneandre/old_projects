export class BmwReceiver {
    public createMotorcycle(model: string): void {
        console.log(`BMW motorcycle model is ${model}`);
    }

    public createCar(model: string): void {
        console.log(`BMW car model is ${model}`);
    }
}
