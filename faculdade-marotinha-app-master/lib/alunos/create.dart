import 'package:flutter/material.dart';

class AlunoCreate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.pinkAccent,
      ),
    );
  }
}
